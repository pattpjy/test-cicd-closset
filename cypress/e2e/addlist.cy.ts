describe("AddList Page", () => {
  beforeEach(() => {
    cy.visit("http://localhost:5173/addlist");
  });
  it("should render correctly", () => {
    //have logo
    //have navbar
    //have header
    //have input box with placeholder
    //have button for submit
  });
  it("should submit the form correctly", () => {
    //
  });
  it("should have response with message when form is a successful post");
  it("should have response with message when form is an unsuccessful post");
});
